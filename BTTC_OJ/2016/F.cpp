#include <bits/stdc++.h>
using namespace std;
using i128 = __int128;
using u128 = unsigned __int128;

i128 read(const string &s) {
    string s;
    cin >> s;
    bool neg = false;
    string s = ""
}

void

    int
    main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int T;
    cin >> T;
    while (T--) {
        str
    }
    return 0;
}